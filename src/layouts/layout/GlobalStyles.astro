<style is:global>
  :root {
    --color-primary: hsl(204, 51%, 21%);
    --color-light: hsl(240, 25%, 98%);
    --color-dark: hsl(204, 51%, 11%);
    --color-accent: hsl(0, 0%, 92%);
    --color-accent-dark: hsl(204, 52%, 18%);
    --color-text: var(--color-dark);
    --color-text-secondary: var(--color-light);
    --color-link: hsl(204, 52%, 44%);
    --header-width: 320px;
    --transition: 0.2s ease-in-out;
  }

  @media screen and (prefers-color-scheme: dark) {
    :root {
      --color-primary: hsl(204, 51%, 18%);
      --color-accent: var(--color-accent-dark);
      --color-text: var(--color-light);
      --color-link: hsl(204, 52%, 60%);
    }
  }

  body {
    background: var(--color-light);
    color: var(--color-text);
    display: grid;
    font-family: "Poppins", sans-serif;
    grid-template-columns: var(--header-width) 1fr minmax(min-content, 750px) 1fr;
    min-height: 100dvh;
  }

  @media screen and (prefers-color-scheme: dark) {
    body {
      background: var(--color-dark);
    }
  }

  main {
    grid-column-start: 3;
    padding: 32px;
  }

  @media screen and (max-width: 768px) {
    body {
      grid-template-columns: 1fr 576px 1fr;
      grid-template-rows: auto 1fr;
    }

    main {
      grid-column-start: 2;
    }
  }

  @media screen and (max-width: 576px) {
    body {
      grid-template-columns: 1fr;
    }

    main {
      grid-column-start: 1;
    }
  }

  main img {
    display: block;
    object-fit: cover;
    width: 100%;
  }

  h2 {
    font-size: 2.25rem;
    font-weight: 600;
    margin-bottom: 36px;
  }

  a {
    color: var(--color-link);
  }

  ul,
  ol {
    list-style-type: none;
    padding: 0;
  }
</style>
